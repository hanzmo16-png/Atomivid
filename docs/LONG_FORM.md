# ATOMIVID Long Form — internal

Third pipeline. Flag `LONG_FORM_ENABLED` defaults off. Server gate is `assertLongFormAccess` (flag + allowlist). The P0 CLI is `LONG_FORM_P0_CLI=1` on an owner machine only.

Modes stay `curiosity_documentary` and `behavior_essay`. Behavior copy is a tool, not a Marcus Aurelius costume. Transcript rewrite is rejected.

Migration `0016_long_form_internal.sql` from the earlier contract ZIP is **not** in this drop and must not be applied.
